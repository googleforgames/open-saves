steps:
# TODO: replace these targets with actual names when we add the main application
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'build-docker', '.']
  id: init-build-docker
- name: 'build-docker'
  args: ['cmake', '--build', '/build', '--parallel', '8']

# TODO: Enable tests when we have tests
# - name: gcr.io/cloud-builders/bazel
#   args: ['test', '...']
options:
  machineType: 'N1_HIGHCPU_8'